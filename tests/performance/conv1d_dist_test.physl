#!/usr/bin/env physl

define(conv, input1, input2, input3, filter1, filter2, filter3,
    block(
        define(array,
            constant_d(1, list(input1, input2, input3), nil, nil, "", "page")),
        define(kernel,
           constant(1,list(filter1, filter2, filter3))),
        //timer(
           define(out, conv1d_d(array, kernel)),
          //  lambda(time, cout("locality", find_here(), ":", time))
        //)
cout(out)
    )
)
apply(conv, sys_argv)
